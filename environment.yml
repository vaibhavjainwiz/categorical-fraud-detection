---
name: triton_example
channels:
  - conda-forge
  - nvidia
  - rapidsai
dependencies:
  - cudatoolkit=12.2
  - cudf=23.08  # Adjusted to the latest RAPIDS version for CUDA 12.2
  - cuml=23.08  # Same version as `cudf`
  - cupy
  - jupyter
  - kaggle
  - matplotlib
  - numpy
  - pandas
  - pip
  - python=3.8
  - scikit-learn<1.2.0
  - pip:
      - tritonclient[all]
      - protobuf==3.20.1
      - xgboost>=1.5,<1.6
